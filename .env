

POSTGRES_USER=coloringbook
POSTGRES_PASSWORD=zuzucoloringbook
POSTGRES_DB=coloringbook


# Used by the frontend container to reach the backend. Once nginx is in
# front of both, this becomes the public API URL (e.g. https://app.yourdomain.com/api)
# — for now, while testing directly on the VPS, http://localhost:8000 is fine.
NEXT_PUBLIC_API_URL=http://localhost:8000